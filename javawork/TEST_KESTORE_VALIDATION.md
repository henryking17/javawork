This is a short test file added to trigger the CI keystore validation job.

Do not merge. This PR is intended to verify that the `keystore-validation-demo` job fails with a helpful message when signing is expected but no keystore or signing secrets are provided.

Delete this file after verifying the workflow logs.
